'use client';

import { ExpenseTracker } from '@/components/marketing/expense-tracker';

export default function ExpensesPage() {
  return (
    <main className="container mx-auto p-6">
      <ExpenseTracker />
    </main>
  );
}